#include <bits/stdc++.h>

using namespace std;
using ll = long long;
using ull = unsigned long long;

int main(){
    int n, m, k;
    cin >> n >> m >> k;
    vector<int>X(k);
    for (int i = 0; i < k; i++){
        cin >> X[i];
    }
    
    system("pause");
    return 0;
}